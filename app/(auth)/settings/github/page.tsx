import SettingsPlaceholder from "@/components/SettingsPlaceholder"

export default function GithubPage() {
  return <SettingsPlaceholder title="GitHub Integration" description="Connect your repositories to sync PRs, issues, and deployment status directly in Hivespace." />
}
